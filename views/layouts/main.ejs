<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Rayuela Analytics</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        .chart-container {
            margin: 20px auto;
            width: 95%;
            max-width: 1200px;
            position: relative;
            background: #0A0203;
            height: 80vh;
        }

        .star-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
        }

        canvas:not(.star-background) {
            position: relative;
            z-index: 1;
            background: transparent;
        }

        body {
            background: #0A0203;
        }
    </style>
    <%- defineContent('style') %>
</head>
<body>
    <%- include('../partials/header') %>
    <main class="main-content">
        <%- body %>
    </main>
    <%- defineContent('script') %>
</body>
</html>

